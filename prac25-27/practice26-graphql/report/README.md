# Отчет по практике 26

Сервер запускается командой:

```bash
npm run start
```

Адрес Apollo Sandbox: `http://localhost:4000/`.

Проверочные операции находятся в `queries.graphql`. Скриншот с тремя запросами и ответами: `apollo-sandbox-checks.png`.
